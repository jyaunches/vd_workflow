# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Overview

This is a Claude Code plugin (`chrysalis`) that provides spec-driven development workflow commands and agents. It implements a structured approach to feature development: Spec Creation → Spec Review → TDD Implementation → Validation.

## Repository Structure (Marketplace Layout)

```
chrysalis/                    # Marketplace root
├── .claude-plugin/
│   └── marketplace.json              # Marketplace manifest
├── plugins/
│   └── chrysalis/            # Plugin directory
│       ├── .claude-plugin/
│       │   └── plugin.json           # Plugin manifest
│       ├── commands/                 # Slash commands
│       │   ├── spec.md               # Create new specification
│       │   ├── execute-wf.md         # Run full automated workflow
│       │   └── execute-wf/           # Workflow support commands
│       ├── agents/                   # Agent definitions
│       ├── skills/                   # Expert skills
│       ├── hooks/                    # Session hooks
│       └── shared_docs/              # Shared documentation
├── CLAUDE.md                         # This file
└── README.md                         # User documentation
```

## Spec Directory Structure

Specifications use a directory-based structure:

```
specs/
└── YYYY-MM-DD_<feature_name>/
    ├── spec.md          # Main specification
    ├── tests.md         # Test specification (generated by review phase)
    └── validation.md    # BDD validation plan (generated by review phase)
```

All commands take a `<spec_dir>` argument (e.g., `specs/2024-01-15_user_auth/`) and derive internal paths automatically.

## Key Workflows

### Complete Feature Workflow
`/chrysalis:execute-wf <spec_dir>` runs the full automated three-phase workflow:

1. **Review Phase** (review-executor agent):
   - Simplify spec → Generate test spec → Generate validation plan
   - **User approval of validation plan**
   - Design review → Implementation review

2. **Implementation Phase** (feature-writer agent):
   - TDD phase-by-phase implementation
   - Check work after all phases complete

3. **Validation Phase** (validation-executor agent):
   - Execute BDD scenarios from validation.md
   - Auto-fix failures with `/bug --auto` (3 attempts)
   - Mark scenarios as `[VALIDATED: sha]`

### Manual Steps
- `/chrysalis:spec <name> "<description>"` - Create new specification directory
- `/chrysalis:execute-wf:implement-phase <spec_dir> [--auto]` - Execute implementation phases
- `/chrysalis:execute-wf:validation-plan <spec_dir>` - Generate BDD validation scenarios
- `/chrysalis:execute-wf:validation-review <spec_dir>` - Review/approve validation plan

## Agent Architecture

Agents are orchestrators that run as subagents to manage multi-step workflows:
- **review-executor** (sonnet): Orchestrates spec review phase (simplify → test spec → validation plan → design review → implementation review)
- **feature-writer** (sonnet): Implements phases using git SHA markers for progress tracking
- **validation-executor** (sonnet): Executes BDD scenarios with auto-fix loop

## Validation System

Validation uses a **parallel artifact** approach:

1. **validation.md** - BDD-style scenarios in Given/When/Then format
2. **Scenario types**: Happy paths + obvious sad paths
3. **Status markers**: `[STATUS: pending|in_progress|passed|failed]`
4. **Validation marker**: `[VALIDATED: sha]` added on pass
5. **Auto-fix**: On failure, `/bug --auto` invoked (up to 3 attempts)

### BDD Scenario Format
```markdown
### Scenario 1.1: User Login Flow [STATUS: pending]
**Type**: Happy Path

**Given**: User has valid credentials
**When**: User submits login form
**Then**: User is redirected to dashboard

**Validation Steps**:
1. **Setup**: Playwright: Navigate to login page
2. **Execute**: Playwright: Fill and submit credentials
3. **Verify**: Playwright: Check dashboard URL

**Tools Required**: Playwright MCP
```

## Skills

Skills provide domain expertise:
- **validation-expert**: Validation tool catalog and deployment-specific patterns

## Specification Format Requirements

Phase headers must follow this exact format for `/implement-phase` compatibility:
```markdown
## Phase 1: [Name]
## Phase 2: [Name] [COMPLETED: git-sha]
```

All specs must include a final "Clean the House" phase for post-implementation cleanup.

The plugin includes `shared_docs/PATTERNS.md` which defines patterns for auto-apply decisions during spec reviews.

## Plugin Cache Management

When this plugin is updated (new commands, skills, or agents), Claude Code doesn't automatically pick up changes due to caching. This is a known issue (GitHub #14061, #15369, #15642).

**To refresh the plugin after updates:**

```bash
# Clear the plugin cache
rm -rf ~/.claude/plugins/cache/

# Then restart Claude Code sessions
```

This is required after:
- Adding new commands, skills, or agents
- Modifying existing files
- Changing plugin structure

## Key Principles Enforced

- **No backward compatibility** - Always direct integration, no parallel systems
- **TDD approach** - Write failing tests first
- **Phase-based implementation** - Incremental, buildable phases
- **No code in specs** - Architecture described conceptually only
- **BDD validation** - Given/When/Then scenarios as first-class artifacts
